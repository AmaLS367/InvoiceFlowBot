line-length = 100
target-version = "py311"

[lint]
select = [
    "E",  # pycodestyle errors
    "F",  # pyflakes
    "I",  # import order
]
ignore = [
    "E501",  # long lines, пока не трогаем
]

[format]
quote-style = "double"
indent-style = "space"
line-ending = "lf"

