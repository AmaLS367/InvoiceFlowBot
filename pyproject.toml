[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "invoiceflowbot"
version = "0.0.0"
description = "Telegram bot for automated invoice processing using OCR"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools]
packages = [
    "domain",
    "services",
    "ocr",
    "storage",
    "handlers",
]

