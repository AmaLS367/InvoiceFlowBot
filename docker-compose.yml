services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    image: invoiceflowbot:latest
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data.sqlite:/app/data.sqlite
      - ./logs:/app/logs
